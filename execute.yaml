# [START execute-sql]
steps:
- name: gcr.io/cloud-builders/docker
  args: ['run', '--network=host', 'postgres:11', 'psql', '-h', 'localhost', '-U', '${_DB_USER}', '-f', '${_SQL_FILE}']
  env:
  - 'DB_USER=quickstart-user'
  - 'SQL_FILE=gs:\\bucket-shu\\sqlscripts\\table.sql'

